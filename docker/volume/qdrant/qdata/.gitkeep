This folder holds Qdrant's vector storage data for the `cvranker-qdrant`
container defined in `docker/docker-compose.yml`.

Contents are gitignored (only this placeholder is tracked) so each
developer/environment gets its own local vector database state.

